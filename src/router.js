export const router = async (request, response) => {
    const {method, url} = request

    if (url === '/login' && method === 'GET') {
       
    }else if (url === '/cadastro' && method === 'POST') {
       
    }else if(url.endsWith('.css')){
        
    }
}